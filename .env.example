ANTHROPIC_API_KEY=sk-ant-...

# Filled in automatically on first run by src/lib/managed-agent.ts.
# Persist these to avoid recreating the agent/environment on every cold start.
AGENT_ID=
ENVIRONMENT_ID=
